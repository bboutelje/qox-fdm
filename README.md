# qox-fdm

This is a sample repository of the key finite difference components in the QoX library. As can be seen from the engineering, it's all statically dispatched for performance. It's also designed to work with a specific combination of components to enforce correctness at compile time.
